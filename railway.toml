[build]
builder = "nixpacks"
buildCommand = "cd apps/backend && pnpm install && pnpm run build"


[deploy]
startCommand = "cd apps/backend && pnpm run deploy && pnpm start"

[build.nixpacksPlan.phases.install]
cmds = ["pnpm i"]